%ul= menu_item(true, 'Панель управления')
%ul.child
  = menu_item(can?(:index, Order), 'Заказы', admin_orders_path)
  = menu_item(can?(:index, Cart), 'Корзины', admin_carts_path)
  = menu_item(can?(:manage, Product), 'Товары', admin_products_path)
  = menu_item(can?(:manage, User), 'Пользователи', admin_users_path)
  = menu_item(can?(:manage, @article), 'Статьи', admin_articles_path)
  = menu_item(can?(:manage, MenuItem), 'Меню', admin_menu_items_path)
  = menu_item(can?(:manage, Mailer), 'Рассылки', admin_mailers_path)
  = menu_item(can?(:force_update, Product), 'Обновление', force_update_path)