- if (messages = flash.to_hash.with_indifferent_access.slice('danger', 'error', 'warning', 'notice', 'success')).any?
  - messages.each do |key, value|
    .div{class: ('alert alert-dismissable  alert-' + key)}
      = button_tag "×", class: 'close', type: 'button', data: {dismiss: 'alert'}, 'aria-hidden' => 'true'
      = value
